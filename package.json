{
	"name": "agent-comms",
	"version": "0.2.1",
	"type": "module",
	"private": true,
	"workspaces": [
		"packages/*"
	],
	"scripts": {
		"service": "bun run packages/service/src/index.ts",
		"cli": "bun run packages/cli/src/index.ts",
		"test": "bun test",
		"lint": "biome check --write .",
		"format": "biome format --write .",
		"typecheck": "bun x tsc --noEmit"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.4.4",
		"@types/bun": "latest",
		"typescript": "^5.9.3"
	}
}
